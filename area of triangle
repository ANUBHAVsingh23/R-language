 calculate_triangle_area <- function(base, height) {
+ 0.5 * base * height
+ }
> base <- 6
> height<-8
> area <- calculate_triangle_area(base, height)
> print(paste("The area of the triangle with base", base, "and height", height, "is", area))
